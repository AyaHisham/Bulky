@model List<Product>
<div class="card shadow border-0 my-4">
    <div class="card-header bg-primary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Product List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6 text-primary"></div>
            @* <div class="col-6 text-end"><a asp-area="Admin" asp-controller="Product" asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Add New Product</a></div> *@
            <div class="col-6 text-end"><a asp-area="Admin" asp-controller="Product" asp-action="Upsert" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Add New Product</a></div>
        </div>
        <table id="tblData" class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>ISBN</th>
                    <th>Author</th>
                    <th>Price</th>
                    <th>Category</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
               @*  @foreach (var currentProduct in Model)
                {
                    <tr>
                        <td>@currentProduct.Title</td>
                        <td>@currentProduct.ISBN</td>
                        <td>@currentProduct.Author</td>
                        <td>@currentProduct.ListPrice</td>
                        <td>@currentProduct.Category.Name</td>
                        <td>
                            <div class="w-75 btn-group" role="group">
                                @* <a asp-area="Admin" asp-controller="Product" asp-action="Edit" asp-route-id="@currentProduct.Id" class="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i> Edit</a> *@
                                @* <a asp-area="Admin" asp-controller="Product" asp-action="Upsert" asp-route-id="@currentProduct.Id" class="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i> Edit</a>
                                <a asp-area="Admin" asp-controller="Product" asp-action="Delete" asp-route-id="@currentProduct.Id" class="btn btn-danger mx-2"><i class="bi bi-trash3"></i> Delete</a>
                            </div>
                        </td>
                    </tr>
                } *@
            </tbody>
        </table>
    </div> 
</div>

@section Scripts{
    <script src="~/js/product.js"></script>
}                                